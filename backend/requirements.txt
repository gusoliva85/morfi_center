# ============================================================
#  Morfi Center · dependencias del backend
#  Python 3.11+   ·   instalar con:  pip install -r requirements.txt
# ============================================================

# --- Web / API -------------------------------------------------
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
python-multipart>=0.0.20          # formularios / subida de comprobantes

# --- Base de datos -------------------------------------------
SQLAlchemy>=2.0.36,<2.1
alembic>=1.14,<2.0

# --- Validación / configuración -----------------------------
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0           # carga de backend/.env

# --- Seguridad / auth --------------------------------------
passlib[bcrypt]>=1.7.4,<2.0
bcrypt>=4.0.1,<4.1               # pin: evita el warning de passlib con bcrypt >= 4.1
PyJWT>=2.10,<3.0
Authlib>=1.4,<2.0                 # OAuth2 / OIDC (Google)

# --- HTTP client (servicios externos) ----------------------
httpx>=0.28,<0.29                 # geocodificación, rutas, tests

# --- Tareas programadas -----------------------------------
APScheduler>=3.11,<4.0

# --- Rate limiting ----------------------------------------
slowapi>=0.1.9,<0.2

# ============================================================
#  Desarrollo y testing
# ============================================================
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<0.25
ruff>=0.8,<1.0
black>=24.10,<25.0
