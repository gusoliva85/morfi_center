# Morfi Center · Usuarios de prueba

Contraseñas simples a propósito: son **solo para pruebas** en desarrollo, nunca para producción. Se generan (idempotente) con `python -m app.db.seed` y se documentan acá a medida que cada fase los agrega — todavía no hay ninguno, porque `run_seed()` sigue vacío (ver `T-0.3.4`); se completa desde `T-1.10.1` (Fase 1).

| Rol | Email | Password | Notas |
|---|---|---|---|
| — | — | — | — |
